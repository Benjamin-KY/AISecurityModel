# requirements.txt
# Minimal dependency set for the Hugging Face Space (app.py only).
# For running the 15-notebook course locally or in Colab, install
# `requirements-notebooks.txt` instead — it includes pandas, matplotlib,
# seaborn, streamlit, bitsandbytes, Pillow, pytesseract, cryptography,
# and other deps the notebooks need but the Space does not.

transformers>=4.36.0
torch>=2.0.0
gradio>=4.0.0
accelerate>=0.25.0
peft>=0.7.0
